java:
  - openjdk17

before_install:
  - chmod +x mvnw

install:
  - ./mvnw clean install -DskipTests=true -Dmaven.javadoc.skip=true

branches:
  only:
    - main

cache:
  directories:
    - $HOME/.m2

#deploy:
#  provider: releases
#  api_key: $GITHUB_TOKEN
#  file: api/target/MaskedCratesAPI-${version}.jar
#  skip_cleanup: true
#  on:
#    tags: true
